(function() {
    'use strict';

    angular
        .module('club')
        .factory('Club', club);

    club.$inject = [];

    function factory(dependencies) {
        var service = {
            function: function
        };

        return service;

        function function() {

        }
    }
})();
